{
    "version": "0.2.0",
    "debugServer": 4711,
    "configurations": [
        {
            "name": "Debug",
            "request": "launch",
            "type": "lldb",
            "preLaunchTask": "Build",
            "program": "${workspaceRoot}/out/${fileBasename}"
        }
    ]
}
{
    "version": "0.2.0",
    "debugServer": 4711,
    "configurations": [
        {
            "name": "Debug",
            "request": "launch",
            "type": "lldb",
            "preLaunchTask": "out/${fileBasename}",
            "program": "${workspaceRoot}/out/${fileBasename}"
        }
    ]
}